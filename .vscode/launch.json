{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start dev server (npm start)",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm start",
            "cwd": "${workspaceFolder}",
            "env": {
                "BROWSER": "none"
            }
        }
    ]
}